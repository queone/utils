# AC14 Clean up README placeholders and stale sub-README references

`README.md` has lingering `<description>` placeholders and references to `cmd/*/README.md` files that do not exist. Replace every placeholder with a one-line summary, remove the references to non-existent per-utility READMEs, and ensure `cash5` is listed. Per-utility READMEs are out of scope unless explicitly separated into a later AC. Low priority.

## Summary

Doc-only cleanup pass over the root `README.md`. Replace `<description>` placeholders with real one-line summaries; drop references to `cmd/*/README.md` files that do not exist; confirm `cash5` is on the alphabetical utilities list.

## Objective Fit

1. **Which part of the primary objective?** TBD — requires scoping.
2. **Why not advance a higher-priority task instead?** TBD — requires scoping.
3. **What existing decision does it depend on or risk contradicting?** TBD — requires scoping.
4. **Intentional pivot?** TBD — requires scoping.

## In Scope

- `README.md` — replace `<description>` placeholders with one-line summaries.
- `README.md` — remove references to non-existent `cmd/*/README.md` files.
- `README.md` — confirm `cash5` appears in the utilities list (alphabetical).

## Out Of Scope

TBD — requires scoping before critique gate.

## Implementation Notes

Rudimentary stub — requires further scoping before critique gate or implementation authorization.

## Acceptance Tests

TBD — requires scoping before critique gate.

## Status

`PENDING` — awaiting further scoping before critique gate or implementation authorization.
